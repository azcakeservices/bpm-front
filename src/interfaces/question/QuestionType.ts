export interface QuestionType {
  id: string;
  order: number;
  label: string;
  isRequired: boolean;
  placeHolder: string;
  inputTypeId: string;
  isActive: boolean;
  createdBy: string;
  createdDate: string;
}
